name: miyazaki_transport_app

description: A new Flutter project.

publishTo: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3

flutter:
  uses-material-design: true
